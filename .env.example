# Local development: copy to `.env` or `.env.local` (see Vite env loading).
# Only variables prefixed with VITE_ are exposed to client-side code.
#
# No environment variables are required for a standard build today.
# Add new lines here as VITE_* keys when the app needs build-time config.
